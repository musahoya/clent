# 🚀 서브 에이전트 데모 사용 가이드

## 빠른 시작

### 1. 데모 프로그램 실행

```bash
# Node.js가 설치되어 있어야 합니다
node demo.js

# 또는
npm start
```

실행하면 메인 에이전트가 4개의 서브 에이전트를 순차적으로 호출하여
웹사이트 제작 프로세스를 시뮬레이션하는 모습을 볼 수 있습니다!

## 📚 프로젝트 구조

```
.
├── README.md              # 서브 에이전트 개념 설명
├── GUIDE.md              # 이 파일 (사용 가이드)
├── demo.js               # 데모 프로그램 (실행 가능)
├── package.json          # npm 설정
│
└── .claude/
    └── agents/           # Claude Code 서브 에이전트 예제
        ├── ui-designer.md      # UI/UX 디자인 전문 에이전트
        ├── code-reviewer.md    # 코드 리뷰 전문 에이전트
        └── api-builder.md      # API 설계 전문 에이전트
```

## 🎯 서브 에이전트란?

### 핵심 개념

**서브 에이전트**는 복잡한 작업을 전문화된 작은 에이전트로 나누는 패턴입니다.

```
큰 작업 (예: 웹사이트 만들기)
    ↓
메인 에이전트가 작업을 분석
    ↓
전문 서브 에이전트들에게 위임
    ↓
각 서브 에이전트가 자신의 전문 분야만 처리
    ↓
결과를 다시 메인 에이전트에게 반환
    ↓
최종 결과 완성
```

### 장점

1. **컨텍스트 효율성** 🧠
   - LLM의 컨텍스트 윈도우를 효율적으로 사용
   - 각 에이전트는 필요한 정보만 받음

2. **전문화** 🎓
   - 각 에이전트가 특정 작업에 최적화
   - 더 정확하고 품질 높은 결과

3. **재사용성** ♻️
   - 한 번 만든 서브 에이전트를 여러 프로젝트에서 활용
   - 팀 전체가 공유 가능

4. **확장성** 📈
   - 새로운 전문 에이전트를 쉽게 추가
   - 병렬 처리 가능

## 💻 데모 프로그램 이해하기

### demo.js의 구조

```javascript
// 1. 서브 에이전트 정의
class PlannerAgent {
  // 기획 전문 에이전트
  async execute(task) { /* ... */ }
}

class DesignerAgent {
  // 디자인 전문 에이전트
  async execute(plan) { /* ... */ }
}

class DeveloperAgent {
  // 개발 전문 에이전트
  async execute(plan, design) { /* ... */ }
}

class QAAgent {
  // 테스트 전문 에이전트
  async execute(code) { /* ... */ }
}

// 2. 메인 에이전트 (오케스트레이터)
class MainAgent {
  async executeTask(userRequest) {
    // 서브 에이전트들을 순서대로 호출
    const plan = await this.subAgents.planner.execute(userRequest);
    const design = await this.subAgents.designer.execute(plan);
    const code = await this.subAgents.developer.execute(plan, design);
    const testResults = await this.subAgents.qa.execute(code);

    return { plan, design, code, testResults };
  }
}
```

### 데이터 흐름

```
사용자 요청: "웹사이트 만들어주세요"
    ↓
📋 기획 에이전트
    → 출력: { pages: [...], features: [...], timeline: '2주' }
    ↓
🎨 디자인 에이전트
    → 입력: 기획 결과
    → 출력: { colorScheme: {...}, components: [...] }
    ↓
💻 개발 에이전트
    → 입력: 기획 결과 + 디자인 결과
    → 출력: { files: 9, components: 5, linesOfCode: 1247 }
    ↓
🧪 QA 에이전트
    → 입력: 개발 결과
    → 출력: { unitTests: {...}, coverage: '94%' }
    ↓
✅ 최종 결과
```

## 🛠️ Claude Code에서 실제로 사용하기

### 1. 서브 에이전트 파일 구조

`.claude/agents/` 디렉토리에 마크다운 파일을 만듭니다:

```markdown
# [에이전트 이름]

당신은 [전문 분야] 전문 에이전트입니다.

## 전문 분야
- 항목 1
- 항목 2

## 작업 방식
1. 단계 1
2. 단계 2

## 산출물
- 산출물 1
- 산출물 2
```

### 2. 서브 에이전트 호출 방법

Claude Code CLI에서:

```bash
# Explore 서브 에이전트 사용
"코드베이스에서 인증 관련 파일을 찾아줘"
→ Claude Code가 자동으로 Explore 에이전트를 호출

# Plan 서브 에이전트 사용
"새로운 결제 시스템을 추가하고 싶어"
→ Claude Code가 Plan 에이전트를 호출하여 계획 수립

# 커스텀 서브 에이전트 사용
"UI 디자인을 검토해줘"
→ .claude/agents/ui-designer.md 에이전트를 호출
```

### 3. 예제: 커스텀 서브 에이전트 만들기

**시나리오**: SEO 최적화 전문 에이전트

파일: `.claude/agents/seo-optimizer.md`

```markdown
# SEO 최적화 에이전트

당신은 웹사이트 SEO 최적화 전문 에이전트입니다.

## 전문 분야
- 메타 태그 최적화
- 구조화된 데이터 (Schema.org)
- 페이지 속도 최적화
- 시맨틱 HTML
- 사이트맵 및 robots.txt

## 작업 순서
1. HTML 구조 분석
2. 메타 태그 검증
3. 이미지 alt 태그 확인
4. 내부 링크 구조 검토
5. 개선 제안 작성

## 산출물
- SEO 체크리스트
- 개선이 필요한 항목 목록
- 우선순위별 액션 아이템
```

## 🎨 실전 활용 팁

### 1. 자주 사용하는 프롬프트를 에이전트로 전환

```markdown
# 변환 전: 매번 프롬프트 입력
"이 코드를 React 컴포넌트로 변환하고,
TypeScript를 사용하고,
Tailwind로 스타일링하고,
접근성을 고려해서 만들어줘"

# 변환 후: 서브 에이전트 생성
.claude/agents/react-converter.md를 만들고
위 요구사항을 에이전트 지침으로 저장
```

### 2. 파이프라인 구축

여러 서브 에이전트를 연결하여 자동화 파이프라인 구축:

```
웹페이지 스크린샷
    ↓
이미지 분석 에이전트
    ↓
컴포넌트 추출 에이전트
    ↓
코드 생성 에이전트
    ↓
테스트 생성 에이전트
    ↓
최종 컴포넌트 완성
```

### 3. 컨텍스트 엔지니어링

```javascript
// ❌ 나쁜 예: 모든 정보를 한 번에 처리
"전체 코드베이스를 분석하고 리팩토링해줘"

// ✅ 좋은 예: 작업을 나누어 처리
1. Explore 에이전트 → 코드 구조 파악
2. Code Reviewer 에이전트 → 문제점 식별
3. Developer 에이전트 → 리팩토링 실행
4. QA 에이전트 → 테스트 검증
```

## 📊 성능 비교

### 전통적인 방법 vs 서브 에이전트

| 항목 | 전통적 방법 | 서브 에이전트 |
|------|-------------|---------------|
| 컨텍스트 사용 | 비효율적 | 효율적 |
| 정확도 | 중간 | 높음 |
| 재사용성 | 낮음 | 높음 |
| 확장성 | 어려움 | 쉬움 |
| 병렬 처리 | 불가능 | 가능 |

## 🔍 더 알아보기

### 관련 개념

- **컨텍스트 윈도우**: LLM이 한 번에 처리할 수 있는 토큰 수
- **프롬프트 체이닝**: 여러 프롬프트를 연결하여 복잡한 작업 수행
- **에이전트 오케스트레이션**: 여러 에이전트를 조율하는 패턴

### 추천 학습 순서

1. ✅ 이 데모 프로그램 실행해보기
2. ✅ README.md의 개념 이해하기
3. ✅ .claude/agents/ 예제 살펴보기
4. 📝 자신만의 서브 에이전트 만들어보기
5. 🚀 실제 프로젝트에 적용하기

## 💡 자주 묻는 질문

### Q1: 서브 에이전트와 일반 함수의 차이는?

**A**: 서브 에이전트는 AI 모델이 실행하는 지능형 작업입니다.
일반 함수는 정해진 로직을 따르지만, 서브 에이전트는 상황에 따라
다르게 판단하고 행동할 수 있습니다.

### Q2: 언제 서브 에이전트를 사용해야 하나요?

**A**: 다음과 같은 경우에 유용합니다:
- 복잡한 작업을 여러 단계로 나눌 수 있을 때
- 특정 작업에 전문적인 지식이 필요할 때
- 같은 작업을 여러 프로젝트에서 반복할 때
- 컨텍스트 제한에 부딪힐 때

### Q3: 서브 에이전트를 너무 많이 만들면 관리가 어렵지 않나요?

**A**: 시작은 3-5개의 핵심 에이전트로 시작하세요:
- 코드 리뷰어
- UI 디자이너
- API 빌더
- 테스트 생성기
- 문서화 도구

필요할 때마다 점진적으로 추가하는 것이 좋습니다.

## 🎯 다음 단계

1. **데모 실행**: `node demo.js` 실행해서 동작 확인
2. **커스터마이징**: demo.js를 수정하여 다른 시나리오 시도
3. **실전 적용**: 자신의 프로젝트에 맞는 서브 에이전트 만들기
4. **공유**: 유용한 에이전트를 팀과 공유하기

---

**Happy Coding! 🚀**

질문이나 피드백은 이슈로 남겨주세요!
